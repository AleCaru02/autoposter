import { useMemo, useState } from 'react';
import { Badge, Card, EmptyState, MetricCard, PageHeader } from '../components/ui';
import { useLocalE2E } from '../services/local-e2e';

type AdminSnapshot={users:any[];tenants:any[];members:any[];plans:any[];subscriptions:any[];overrides:any[];usage:any[];aiUsage:any[];aiBudgets:any[];jobs:any[];connections:any[];audit:any[];deletions:any[]};
const money=(microunits:number)=>`$${(microunits/1_000_000).toFixed(4)}`;

export function AdminCustomersPage(){
  const local=useLocalE2E();const [data,setData]=useState<AdminSnapshot|null>(null);const [tenantId,setTenantId]=useState('');const [planCode,setPlanCode]=useState('');const [message,setMessage]=useState<string|null>(null);const [soft,setSoft]=useState('5000000');const [hard,setHard]=useState('10000000');const [overridePosts,setOverridePosts]=useState('');
  const selected=useMemo(()=>data?.tenants.find((item)=>item.id===tenantId)??null,[data,tenantId]);
  const load=async()=>{try{const next=await local.api<AdminSnapshot>('/admin/customers');setData(next);if(!tenantId&&next.tenants[0])setTenantId(String(next.tenants[0].id));if(!planCode&&next.plans[0])setPlanCode(String(next.plans[0].code));setMessage(null);}catch(e){setMessage(e instanceof Error?e.message:String(e));}};
  const claim=async()=>{await local.api('/dev/grant-platform-admin',{method:'POST'});await load();};
  const act=async(path:string,method:'POST'|'PATCH',body:unknown)=>{try{await local.api(path,{method,body:JSON.stringify(body)});setMessage('Modifica salvata lato server.');await load();}catch(e){setMessage(e instanceof Error?e.message:String(e));}};
  if(!local.enabled)return <><PageHeader eyebrow="Admin" title="Clienti e piani" description="Backend reale non collegato. Nessun dato demo viene presentato come cliente reale."/><Card><EmptyState title="NON COLLEGATO" body="La console admin richiede il backend Supabase/API."/></Card></>;
  const failedJobs=data?.jobs.filter((job)=>job.status==='failed').length??0;const estimated=(data?.aiUsage??[]).reduce((sum,event)=>sum+Number(event.actual_cost_microunits??event.estimated_cost_microunits??0),0);
  return <><PageHeader eyebrow="Platform admin · manual billing" title="Clienti, piani, quote e operazioni" description="Stripe è OFF. Tutte le modifiche passano dal backend e richiedono platform-admin RBAC." action={<div className="card-actions"><button className="button secondary" onClick={()=>void claim()}>Claim admin locale</button><button data-testid="load-admin" className="button" onClick={()=>void load()}>Aggiorna</button></div>}/>{message&&<Card><p role="status">{message}</p></Card>}{!data?<Card><EmptyState title="Carica la console" body="Nessuna mutation viene eseguita dal browser senza controllo RBAC server-side."/></Card>:<><div className="metric-grid"><MetricCard label="Utenti Auth" value={String(data.users.length)} hint="Supabase Auth admin"/><MetricCard label="Tenant" value={String(data.tenants.length)} hint={`${data.tenants.filter((t)=>t.status==='suspended').length} sospesi`}/><MetricCard label="AI ledger" value={money(estimated)} hint={`${data.aiUsage.length} eventi`}/><MetricCard label="Scheduler failures" value={String(failedJobs)} hint={`${data.jobs.length} job persistiti`}/></div><div className="two-col"><Card><h2>Seleziona cliente</h2><label>Tenant<select value={tenantId} onChange={(e)=>setTenantId(e.target.value)}>{data.tenants.map((tenant)=><option key={tenant.id} value={tenant.id}>{tenant.name} · {tenant.data_mode??'DEMO'} · {tenant.status}</option>)}</select></label>{selected&&<div className="signal-list"><div className="signal-row"><span>Modalità dati</span><Badge tone={selected.data_mode==='REAL'?'good':'info'}>{selected.data_mode??'DEMO'}</Badge></div><div className="signal-row"><span>Stato</span><Badge tone={selected.status==='active'?'good':'warn'}>{selected.status}</Badge></div><div className="signal-row"><span>Membership</span><strong>{data.members.filter((m)=>m.tenant_id===tenantId&&m.status==='active').length}</strong></div><div className="signal-row"><span>Connessioni</span><strong>{data.connections.filter((c)=>c.tenant_id===tenantId).length}</strong></div></div>}</Card><Card><h2>Piano manuale</h2><label>Piano<select value={planCode} onChange={(e)=>setPlanCode(e.target.value)}>{data.plans.map((plan)=><option key={plan.id} value={plan.code}>{plan.name} · {plan.code}</option>)}</select></label><button className="button" disabled={!tenantId||!planCode} onClick={()=>void act(`/admin/tenants/${tenantId}/plan`,'POST',{planCode})}>Assegna / modifica piano</button><p className="muted">Provider subscription: manual. Nessun checkout Stripe.</p></Card></div><div className="three-col"><Card><h2>Accesso tenant</h2><div className="card-actions"><button className="button secondary" onClick={()=>void act(`/admin/tenants/${tenantId}/status`,'PATCH',{status:'suspended'})}>Sospendi</button><button className="button" onClick={()=>void act(`/admin/tenants/${tenantId}/status`,'PATCH',{status:'active'})}>Riattiva</button></div></Card><Card><h2>DEMO / REAL</h2><p>Il passaggio DEMO → REAL elimina fixture social, analytics e published mock del tenant.</p><div className="card-actions"><button className="button secondary" onClick={()=>void act(`/admin/tenants/${tenantId}/data-mode`,'PATCH',{dataMode:'DEMO'})}>DEMO</button><button className="button" onClick={()=>void act(`/admin/tenants/${tenantId}/data-mode`,'PATCH',{dataMode:'REAL'})}>REAL</button></div></Card><Card><h2>Override quota</h2><label>Post/settimana<input value={overridePosts} onChange={(e)=>setOverridePosts(e.target.value)} placeholder="es. 7" inputMode="numeric"/></label><button className="button secondary" disabled={!overridePosts} onClick={()=>void act(`/admin/tenants/${tenantId}/overrides`,'PATCH',{overrides:{posts_per_week:Number(overridePosts)},reason:'Manual admin quota'})}>Salva override</button></Card></div><div className="two-col"><Card><h2>Budget AI tenant</h2><label>Soft limit microunits<input value={soft} onChange={(e)=>setSoft(e.target.value)}/></label><label>Hard limit microunits<input value={hard} onChange={(e)=>setHard(e.target.value)}/></label><button className="button" onClick={()=>void act(`/admin/tenants/${tenantId}/ai-budget`,'PATCH',{currency:'USD',softLimitMicrounits:Number(soft),hardLimitMicrounits:Number(hard),enabled:true})}>Imposta budget hard/soft</button><p className="muted">OpenAI resta OFF: il budget è preparato prima delle chiamate reali.</p></Card><Card><h2>Operazioni e failures</h2><div className="signal-row"><span>Uso tenant</span><strong>{data.usage.filter((x)=>x.tenant_id===tenantId).length} counter</strong></div><div className="signal-row"><span>AI events</span><strong>{data.aiUsage.filter((x)=>x.tenant_id===tenantId).length}</strong></div><div className="signal-row"><span>Job falliti</span><strong>{data.jobs.filter((x)=>x.tenant_id===tenantId&&x.status==='failed').length}</strong></div><div className="signal-row"><span>Audit entries</span><strong>{data.audit.filter((x)=>x.tenant_id===tenantId).length}</strong></div></Card></div><Card><h2>Richieste cancellazione</h2>{data.deletions.length===0?<p className="muted">Nessuna richiesta.</p>:data.deletions.map((item)=><div className="signal-row" key={item.id}><span>{item.scope} · {item.status}</span><strong>{item.requesting_user_id}</strong></div>)}</Card></>}</>;
}
