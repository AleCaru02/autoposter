name: Audit Viewer Authenticated Runtime

on:
  workflow_dispatch:

concurrency:
  group: audit-viewer-authenticated-runtime
  cancel-in-progress: false

jobs:
  runtime:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout selected verifier ref
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install
        run: npm install --no-audit --no-fund

      - name: Pin certified production base and verification-only scope
        shell: bash
        run: |
          set -euo pipefail
          expected="dbb20019c0e2d75780284b66584cf07845b21515"
          if [ "$GITHUB_REF_NAME" != "verify/image-generate-gating-runtime" ]; then
            echo "RUNTIME_VERIFIER_NOT_CERTIFIED: wrong branch $GITHUB_REF_NAME" >&2
            exit 1
          fi
          merge_base=$(git merge-base HEAD "$expected")
          if [ "$merge_base" != "$expected" ]; then
            echo "RUNTIME_VERIFIER_NOT_CERTIFIED: production base mismatch $merge_base" >&2
            exit 1
          fi
          changed=$(git diff --name-only "$expected"...HEAD)
          printf '%s\n' "$changed" > image-generate-verifier-diff.txt
          while IFS= read -r path; do
            [ -z "$path" ] && continue
            case "$path" in
              .github/workflows/audit-viewer-runtime.yml|.github/workflows/gate.yml|tests/image-generate-*|tests/audit-viewer-runtime-static.test.mjs|tests/wrangler.image-generate-runtime.jsonc) ;;
              *)
                echo "RUNTIME_VERIFIER_NOT_CERTIFIED: product diff detected: $path" >&2
                exit 1
                ;;
            esac
          done <<< "$changed"
          echo "Image generation verifier scope: PASS"

      - name: Static pre-certification gate
        run: |
          npm run typecheck
          npm run test:entitlements
          npm run test:entitlement-migration
          npm run test:image-generation-gating
          node tests/image-generate-provider-harness.test.mjs
          node tests/image-generate-runtime-static.test.mjs
          npm run test:tenant-security
          npm run test:tenant-cross-security
          npm run build
          npx wrangler versions upload --dry-run
          npx wrangler versions upload --dry-run --config tests/wrangler.image-generate-runtime.jsonc

      - name: Generate ephemeral image generate QA material
        shell: bash
        run: |
          set -euo pipefail
          marker="${GITHUB_RUN_ID}"
          password=$(openssl rand -hex 24)
          token=$(openssl rand -hex 32)
          alias="imageqa-${GITHUB_RUN_ID}"
          echo "::add-mask::$password"
          echo "::add-mask::$token"
          echo "IMAGE_GENERATE_QA_MARKER=$marker" >> "$GITHUB_ENV"
          echo "IMAGE_GENERATE_QA_PASSWORD=$password" >> "$GITHUB_ENV"
          echo "IMAGE_GENERATE_QA_TOKEN_VALUE=$token" >> "$GITHUB_ENV"
          echo "IMAGE_GENERATE_PREVIEW_ALIAS=$alias" >> "$GITHUB_ENV"
          echo "IMAGE_GENERATE_PREVIEW_URL=https://${alias}-autoposter.02alessandrocaruso.workers.dev" >> "$GITHUB_ENV"
          printf 'IMAGE_GENERATE_QA_TOKEN=%s\n' "$token" > .image-generate-runtime-secrets.env
          chmod 600 .image-generate-runtime-secrets.env

      - name: Upload provider-instrumented Preview version
        shell: bash
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          set -euo pipefail
          npx wrangler versions upload \
            --config tests/wrangler.image-generate-runtime.jsonc \
            --keep-vars \
            --preview-alias "$IMAGE_GENERATE_PREVIEW_ALIAS" \
            --secrets-file .image-generate-runtime-secrets.env | tee image-generate-upload.log
          version_id=$(sed -n 's/^Worker Version ID: \([0-9a-fA-F-]*\)$/\1/p' image-generate-upload.log | tail -n 1)
          if ! printf '%s' "$version_id" | grep -Eq '^[0-9a-fA-F]{8}-[0-9a-fA-F-]{27,}$'; then
            echo "RUNTIME_VERIFIER_NOT_CERTIFIED: Preview version id not captured" >&2
            exit 1
          fi
          echo "IMAGE_GENERATE_PREVIEW_VERSION_ID=$version_id" >> "$GITHUB_ENV"
          echo "EPHEMERAL_CONTROLLER_VERSION_ID=$version_id" >> "$GITHUB_ENV"
          chmod 600 .image-generate-runtime-secrets.env || true
          shred -u .image-generate-runtime-secrets.env 2>/dev/null || rm -f .image-generate-runtime-secrets.env

      - name: Verify Preview isolation from production
        shell: bash
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          set -euo pipefail
          curl -fsS \
            -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" \
            "https://api.cloudflare.com/client/v4/accounts/$CLOUDFLARE_ACCOUNT_ID/workers/scripts/autoposter/deployments" \
            -o image-generate-deployments-before.json
          node - <<'NODE'
          const fs=require('node:fs');
          const body=JSON.parse(fs.readFileSync('image-generate-deployments-before.json','utf8'));
          if(body?.success!==true) throw new Error('Cloudflare deployment read failed');
          const result=body.result;
          const deployments=Array.isArray(result?.deployments)?result.deployments:Array.isArray(result)?result:[];
          if(!deployments.length) throw new Error('no production deployments returned');
          const deploymentIds=deployments.map(x=>String(x.id||'')).filter(Boolean).sort();
          const versionIds=[...new Set(deployments.flatMap(x=>Array.isArray(x.versions)?x.versions.map(v=>String(v.version_id||'')):[]).filter(Boolean))].sort();
          const ephemeral=process.env.IMAGE_GENERATE_PREVIEW_VERSION_ID||'';
          if(!ephemeral) throw new Error('Preview version id missing');
          if(versionIds.includes(ephemeral)) throw new Error('Preview version attached to production');
          fs.appendFileSync(process.env.GITHUB_ENV,`IMAGE_GENERATE_PRODUCTION_DEPLOYMENT_IDS=${deploymentIds.join(',')}\nIMAGE_GENERATE_PRODUCTION_VERSION_IDS=${versionIds.join(',')}\n`);
          console.log('IMAGE_GENERATE_PREVIEW_ISOLATION: PASS',JSON.stringify({attachedToProduction:false,productionDeployments:deploymentIds.length}));
          NODE

      - name: Provider instrumentation preflight and stale QA recovery
        shell: bash
        run: |
          set -euo pipefail
          wrong=$(curl -sS -o /dev/null -w "%{http_code}" -X POST \
            -H 'content-type: application/json' \
            -H 'x-image-generate-qa-token: wrong' \
            -H "x-image-generate-qa-marker: $IMAGE_GENERATE_QA_MARKER" \
            --data "{\"action\":\"preflight\",\"marker\":\"$IMAGE_GENERATE_QA_MARKER\"}" \
            "$IMAGE_GENERATE_PREVIEW_URL/__qa/control" || true)
          if [ "$wrong" != "403" ]; then
            echo "RUNTIME_VERIFIER_NOT_CERTIFIED: Preview token gate HTTP $wrong" >&2
            exit 1
          fi

          read_preflight() {
            curl -sS -o image-generate-preflight.json -w "%{http_code}" -X POST \
              -H 'content-type: application/json' \
              -H "x-image-generate-qa-token: $IMAGE_GENERATE_QA_TOKEN_VALUE" \
              -H "x-image-generate-qa-marker: $IMAGE_GENERATE_QA_MARKER" \
              --data "{\"action\":\"preflight\",\"marker\":\"$IMAGE_GENERATE_QA_MARKER\"}" \
              "$IMAGE_GENERATE_PREVIEW_URL/__qa/control"
          }

          status=$(read_preflight)
          if [ "$status" != "200" ]; then
            echo "RUNTIME_VERIFIER_NOT_CERTIFIED: Preview preflight HTTP $status" >&2
            exit 1
          fi
          stale=$(node -e "const b=require('./image-generate-preflight.json');process.stdout.write(String(Number(b.recognizedQaUsers||0)))")
          if [ "$stale" -gt 0 ]; then
            cleanup_status=$(curl -sS -o image-generate-stale-cleanup.json -w "%{http_code}" -X POST \
              -H 'content-type: application/json' \
              -H "x-image-generate-qa-token: $IMAGE_GENERATE_QA_TOKEN_VALUE" \
              -H "x-image-generate-qa-marker: $IMAGE_GENERATE_QA_MARKER" \
              --data "{\"action\":\"cleanup-residue\",\"marker\":\"$IMAGE_GENERATE_QA_MARKER\"}" \
              "$IMAGE_GENERATE_PREVIEW_URL/__qa/control")
            CLEANUP_STATUS="$cleanup_status" node -e 'const fs=require("node:fs");const body=JSON.parse(fs.readFileSync("image-generate-stale-cleanup.json","utf8"));if(process.env.CLEANUP_STATUS!=="200"||body.cleaned!==true||body.recognizedQaUsers!==0){const safe=String(body.detail||body.error||"unknown").replace(/[^A-Za-z0-9_. -]/g," ").slice(0,240);throw new Error(`stale QA cleanup failed HTTP ${process.env.CLEANUP_STATUS}: ${safe}`);}'
            status=$(read_preflight)
          fi

          inst_status=$(curl -sS -o image-generate-instrumentation.json -w "%{http_code}" -X POST \
            -H 'content-type: application/json' \
            -H "x-image-generate-qa-token: $IMAGE_GENERATE_QA_TOKEN_VALUE" \
            -H "x-image-generate-qa-marker: $IMAGE_GENERATE_QA_MARKER" \
            --data "{\"action\":\"instrumentation-status\",\"marker\":\"$IMAGE_GENERATE_QA_MARKER\"}" \
            "$IMAGE_GENERATE_PREVIEW_URL/__qa/control")
          PREFLIGHT_STATUS="$status" INSTRUMENTATION_STATUS="$inst_status" node - <<'NODE'
          const fs=require('node:fs');
          const pre=JSON.parse(fs.readFileSync('image-generate-preflight.json','utf8'));
          const inst=JSON.parse(fs.readFileSync('image-generate-instrumentation.json','utf8'));
          if(process.env.PREFLIGHT_STATUS!=='200') throw new Error(`preflight HTTP ${process.env.PREFLIGHT_STATUS}`);
          if(process.env.INSTRUMENTATION_STATUS!=='200'||inst.armed!==true||inst.previewOnly!==true||inst.promptLogging!==false){
            throw new Error('RUNTIME_VERIFIER_NOT_CERTIFIED: provider instrumentation unarmed');
          }
          for(const type of ['MEDIA_MANAGER','IMAGE','TEXT','PROVIDER_FAILURE','LATENCY','BARRIER','TECHNICAL_LEDGER_FAILURE','ASSET_FAILURE']) if(!inst.supports?.includes(type)) throw new Error(`RUNTIME_VERIFIER_NOT_CERTIFIED: instrumentation ${type}`);
          for(const key of ['qaUsers','recognizedQaUsers','qaProfiles','qaOwners','qaEntitlements','qaUsageEvents','qaUsageBuckets','qaAiEvents','qaAssets','qaStrategies','qaTechnicalOutbox','qaContent','qaVariants','qaJobs','qaProviderCalls']) if(pre[key]!==0) throw new Error(`preflight residue ${key}=${pre[key]}`);
          if(pre.superAdmins!==1||pre.profilesWithoutOwner!==0||pre.multipleOwners!==0||pre.ownerMismatch!==0||pre.openPolicies!==0||pre.anonymousPrivilegedTables!==0) throw new Error('production security baseline failed');
          fs.appendFileSync(process.env.GITHUB_ENV,`IMAGE_GENERATE_BASELINE_PROFILES=${pre.profilesTotal}\nIMAGE_GENERATE_CONTROLLER_READY=true\n`);
          console.log('IMAGE_GENERATE_PROVIDER_INSTRUMENTATION: ARMED',JSON.stringify({previewOnly:true,persistedCounter:inst.persistedCounter,promptLogging:false,realProviderAvailable:pre.realProviderAvailable===true}));
          NODE

      - name: Image generation authenticated runtime
        shell: bash
        run: |
          set -euo pipefail
          node tests/image-generate-runtime.mjs | tee image-generate-runtime-output.log

      - name: Sensitive output scan
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          files=""
          for f in image-generate-runtime-output.log image-generate-upload.log image-generate-preflight.json image-generate-instrumentation.json image-generate-stale-cleanup.json; do
            [ -f "$f" ] && files="$files $f"
          done
          if [ -n "$files" ]; then
            if grep -Eini \
              '(sk-[A-Za-z0-9_-]{16,}|postgres(ql)?://[^[:space:]]+|Authorization:[[:space:]]*Bearer[[:space:]]+[A-Za-z0-9._-]{20,}|Set-Cookie:[[:space:]]*[^[:space:]]+|Cookie:[[:space:]]*[^[:space:]]+|IMAGE_GENERATE_QA_TOKEN_VALUE=[A-Za-z0-9]{16,}|IMAGE_GENERATE_QA_PASSWORD=[A-Za-z0-9]{16,}|DATABASE_URL=[^[:space:]]+)' \
              $files > image-generate-sensitive-findings.txt; then
              cat image-generate-sensitive-findings.txt >&2
              echo "sensitiveFindings > 0" >&2
              exit 1
            fi
          fi
          echo "sensitiveFindings=0"

      - name: Cleanup image generate ephemeral fixtures
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          if [ "${IMAGE_GENERATE_CONTROLLER_READY:-}" != "true" ]; then
            echo "IMAGE_GENERATE_CLEANUP_NOT_ARMED: no QA fixtures authorized"
            exit 0
          fi
          for suffix in a b; do
            status=$(curl -sS -o "image-generate-cleanup-$suffix.json" -w "%{http_code}" -X POST \
              -H 'content-type: application/json' \
              -H "x-image-generate-qa-token: $IMAGE_GENERATE_QA_TOKEN_VALUE" \
              -H "x-image-generate-qa-marker: $IMAGE_GENERATE_QA_MARKER" \
              --data "{\"action\":\"cleanup-user\",\"marker\":\"$IMAGE_GENERATE_QA_MARKER\",\"suffix\":\"$suffix\"}" \
              "$IMAGE_GENERATE_PREVIEW_URL/__qa/control" || true)
            CLEANUP_STATUS="$status" CLEANUP_SUFFIX="$suffix" node - <<'NODE'
          const fs=require('node:fs');
          const suffix=process.env.CLEANUP_SUFFIX;
          const body=JSON.parse(fs.readFileSync(`image-generate-cleanup-${suffix}.json`,'utf8'));
          if(process.env.CLEANUP_STATUS!=='200'||body.cleaned!==true||body.cleanedUsers!==1){const safe=String(body.detail||body.error||'unknown').replace(/[^A-Za-z0-9_. -]/g,' ').slice(0,240);throw new Error(`cleanup user ${suffix} failed HTTP ${process.env.CLEANUP_STATUS}: ${safe}`);}
          NODE
          done
          status=$(curl -sS -o image-generate-cleanup.json -w "%{http_code}" -X POST \
            -H 'content-type: application/json' \
            -H "x-image-generate-qa-token: $IMAGE_GENERATE_QA_TOKEN_VALUE" \
            -H "x-image-generate-qa-marker: $IMAGE_GENERATE_QA_MARKER" \
            --data "{\"action\":\"cleanup\",\"marker\":\"$IMAGE_GENERATE_QA_MARKER\"}" \
            "$IMAGE_GENERATE_PREVIEW_URL/__qa/control" || true)
          CLEANUP_STATUS="$status" node - <<'NODE'
          const fs=require('node:fs');
          const body=JSON.parse(fs.readFileSync('image-generate-cleanup.json','utf8'));
          if(process.env.CLEANUP_STATUS!=='200'||body.cleaned!==true){const safe=String(body.detail||body.error||'unknown').replace(/[^A-Za-z0-9_. -]/g,' ').slice(0,240);throw new Error(`cleanup failed HTTP ${process.env.CLEANUP_STATUS}: ${safe}`);}
          for(const key of ['qaUsers','recognizedQaUsers','qaProfiles','qaOwners','qaAppUsers','qaSessions','qaAccounts','qaEntitlements','qaUsageEvents','qaUsageBuckets','qaAiEvents','qaAssets','qaStrategies','qaTechnicalOutbox','qaContent','qaVariants','qaJobs','qaProviderCalls']) if(body[key]!==0) throw new Error(`cleanup residue ${key}=${body[key]}`);
          if(body.superAdmins!==1) throw new Error(`SUPER_ADMIN post-cleanup ${body.superAdmins}`);
          if(String(body.profilesTotal)!==String(process.env.IMAGE_GENERATE_BASELINE_PROFILES)) throw new Error(`profile baseline mismatch ${body.profilesTotal} != ${process.env.IMAGE_GENERATE_BASELINE_PROFILES}`);
          if(body.profilesWithoutOwner!==0||body.multipleOwners!==0||body.ownerMismatch!==0||body.openPolicies!==0||body.anonymousPrivilegedTables!==0) throw new Error('post-cleanup security invariant failed');
          console.log('IMAGE_GENERATE_CLEANUP: PASS',JSON.stringify({qaUsers:0,qaProfiles:0,qaUsageEvents:0,qaAiEvents:0,qaAssets:0,qaContent:0,qaJobs:0,superAdmins:body.superAdmins,profilesTotal:body.profilesTotal}));
          NODE

      - name: Post-cleanup production and deployment invariants
        if: always()
        shell: bash
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          set -euo pipefail
          curl -fsS https://autoposter.02alessandrocaruso.workers.dev/api/security/tenant-audit -o image-generate-tenant-audit.json
          curl -fsS https://autoposter.02alessandrocaruso.workers.dev/api/security/managed-auth-capabilities -o image-generate-auth-audit.json
          curl -fsS \
            -H "Authorization: Bearer $CLOUDFLARE_API_TOKEN" \
            "https://api.cloudflare.com/client/v4/accounts/$CLOUDFLARE_ACCOUNT_ID/workers/scripts/autoposter/deployments" \
            -o image-generate-deployments-after.json
          node - <<'NODE'
          const fs=require('node:fs');
          const tenant=JSON.parse(fs.readFileSync('image-generate-tenant-audit.json','utf8'));
          const auth=JSON.parse(fs.readFileSync('image-generate-auth-audit.json','utf8'));
          const cf=JSON.parse(fs.readFileSync('image-generate-deployments-after.json','utf8'));
          if(tenant?.ready!==true||tenant?.ownerContract?.ready!==true) throw new Error('tenant invariant not ready');
          if(tenant.ownerContract.profilesWithoutOwner!==0) throw new Error(`profilesWithoutOwner ${tenant.ownerContract.profilesWithoutOwner}`);
          if(auth?.managedAuth?.ready!==true||auth.managedAuth.nativeAdminUsers!==1) throw new Error(`native SUPER_ADMIN ${auth?.managedAuth?.nativeAdminUsers}`);
          if(process.env.IMAGE_GENERATE_BASELINE_PROFILES&&String(tenant.ownerContract.profilesTotal)!==String(process.env.IMAGE_GENERATE_BASELINE_PROFILES)) throw new Error('profile baseline changed');
          if(cf?.success!==true) throw new Error('Cloudflare deployment read failed');
          const result=cf.result;
          const deployments=Array.isArray(result?.deployments)?result.deployments:Array.isArray(result)?result:[];
          const deploymentIds=deployments.map(x=>String(x.id||'')).filter(Boolean).sort();
          const versionIds=[...new Set(deployments.flatMap(x=>Array.isArray(x.versions)?x.versions.map(v=>String(v.version_id||'')):[]).filter(Boolean))].sort();
          const beforeDeployments=String(process.env.IMAGE_GENERATE_PRODUCTION_DEPLOYMENT_IDS||'').split(',').filter(Boolean).sort();
          const beforeVersions=String(process.env.IMAGE_GENERATE_PRODUCTION_VERSION_IDS||'').split(',').filter(Boolean).sort();
          if(JSON.stringify(deploymentIds)!==JSON.stringify(beforeDeployments)||JSON.stringify(versionIds)!==JSON.stringify(beforeVersions)) throw new Error('production deployment changed during verifier');
          if(versionIds.includes(process.env.IMAGE_GENERATE_PREVIEW_VERSION_ID||'')) throw new Error('QA Preview attached to production');
          console.log('IMAGE_GENERATE_POST_CLEANUP_PRODUCTION: PASS',JSON.stringify({nativeAdminUsers:auth.managedAuth.nativeAdminUsers,profilesTotal:tenant.ownerContract.profilesTotal,profilesWithoutOwner:tenant.ownerContract.profilesWithoutOwner,productionDeploymentUnchanged:true}));
          NODE

      - name: Remove local ephemeral credential material
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          if [ -f .image-generate-runtime-secrets.env ]; then
            chmod 600 .image-generate-runtime-secrets.env || true
            shred -u .image-generate-runtime-secrets.env 2>/dev/null || rm -f .image-generate-runtime-secrets.env
          fi
          rm -f image-generate-verifier-diff.txt image-generate-upload.log image-generate-preflight.json image-generate-instrumentation.json image-generate-stale-cleanup.json image-generate-cleanup.json image-generate-cleanup-a.json image-generate-cleanup-b.json image-generate-runtime-output.log image-generate-sensitive-findings.txt image-generate-tenant-audit.json image-generate-auth-audit.json image-generate-deployments-before.json image-generate-deployments-after.json
          echo "IMAGE_GENERATE_EPHEMERAL_SECRET_CLEANUP: PASS"
