import assert from "node:assert/strict";
import { analyze,assertNoSensitive,bootstrap,committed,control,entitlement,marker,reset,reserved,usage,waitProvider } from "./brand-analyze-runtime-lib.mjs";
const evidence=[];const add=(scenario,value)=>{assertNoSensitive(value);evidence.push({scenario,...value});};const{userA,userB,profileA,profileB}=await bootstrap();
await reset(profileA);await entitlement(profileA,"disabled");let response=await analyze(userA.token,profileA,"capability-disabled"),calls=await control("provider-calls",{profileId:profileA,scenario:"capability-disabled"}),snapshot=await usage(profileA);assert.equal(response.status,429);assert.equal(response.body?.error,"CAPABILITY_DISABLED");assert.equal(calls.count,0);assert.equal(snapshot.generic.length,0);add("capability-disabled",{status:response.status,providerCalls:0,logicalCommitted:0});
await reset(profileA);await entitlement(profileA,"limited",0);response=await analyze(userA.token,profileA,"limit-zero");calls=await control("provider-calls",{profileId:profileA,scenario:"limit-zero"});snapshot=await usage(profileA);assert.equal(response.status,429);assert.equal(response.body?.error,"CAPABILITY_LIMIT_REACHED");assert.equal(calls.count,0);assert.equal(snapshot.generic.length,0);add("limit-zero",{status:response.status,providerCalls:0,logicalCommitted:0});
await reset(profileA);await entitlement(profileA,"limited",1);const first=await analyze(userA.token,profileA,"success-and-duplicate"),duplicate=await analyze(userA.token,profileA,"success-and-duplicate");calls=await control("provider-calls",{profileId:profileA,scenario:"success-and-duplicate"});snapshot=await usage(profileA);assert.equal(first.status,200);assert.equal(duplicate.status,200);assert.deepEqual(duplicate.body,first.body);assert.equal(calls.count,1);assert.equal(committed(snapshot),1);assert.equal(reserved(snapshot),0);assert.equal(snapshot.technical.length,1);assert.equal(snapshot.technical[0].operation,"ANALYZE_BRAND_ONBOARDING");assert.equal(snapshot.brandCount,1);assert.equal(snapshot.onboardingCompleted,true);add("success-and-duplicate",{status:200,duplicateStatus:200,providerCalls:1,logicalCommitted:1,technicalEvents:1});
await reset(profileA);await entitlement(profileA,"limited",1);const pending=analyze(userA.token,profileA,"concurrent-duplicate");await waitProvider(profileA,"concurrent-duplicate");const concurrent=await analyze(userA.token,profileA,"concurrent-duplicate");await control("release-barrier",{profileId:profileA,scenario:"concurrent-duplicate",operationId:"brand-concurrent-duplicate"});const completed=await pending;calls=await control("provider-calls",{profileId:profileA,scenario:"concurrent-duplicate"});snapshot=await usage(profileA);assert.equal(completed.status,200);assert.equal(concurrent.status,409);assert.equal(concurrent.body?.error,"BRAND_ANALYSIS_IN_PROGRESS");assert.equal(calls.count,1);assert.equal(committed(snapshot),1);assert.equal(reserved(snapshot),0);add("concurrent-duplicate",{statuses:[200,409],providerCalls:1,logicalCommitted:1});
await reset(profileA);await entitlement(profileA,"unlimited");response=await analyze(userA.token,profileA,"provider-failure-release");calls=await control("provider-calls",{profileId:profileA,scenario:"provider-failure-release"});snapshot=await usage(profileA);assert.equal(response.status,502);assert.equal(response.body?.error,"ONBOARDING_ANALYSIS_FAILED");assert.equal(calls.count,1);assert.equal(snapshot.generic[0].state,"RELEASED");assert.equal(snapshot.technical.length,0);assert.equal(snapshot.brandCount,0);add("provider-failure-release",{status:502,providerCalls:1,released:"RELEASED"});
await reset(profileA);await entitlement(profileA,"unlimited");response=await analyze(userA.token,profileA,"technical-persistence-failure");calls=await control("provider-calls",{profileId:profileA,scenario:"technical-persistence-failure"});snapshot=await usage(profileA);assert.equal(response.status,503);assert.equal(response.body?.error,"METERING_FAILED");assert.equal(calls.count,1);assert.equal(snapshot.generic[0].state,"RELEASED");assert.equal(snapshot.generic[0].technical_usage_state,"PENDING_RECONCILIATION");assert.ok(Number(snapshot.generic[0].technical_outbox_items)>0);assert.equal(snapshot.technical.length,0);assert.equal(snapshot.brandCount,0);add("technical-persistence-failure",{status:503,providerCalls:1,released:"RELEASED",outbox:snapshot.generic[0].technical_outbox_items});
await reset(profileB);await entitlement(profileB,"unlimited");response=await analyze(userA.token,profileB,"cross-tenant-denied");calls=await control("provider-calls",{profileId:profileB,scenario:"cross-tenant-denied"});snapshot=await usage(profileB);assert.equal(response.status,404);assert.equal(response.body?.error,"PROFILE_NOT_FOUND");assert.equal(calls.count,0);assert.equal(snapshot.generic.length,0);add("cross-tenant-denied",{status:404,providerCalls:0,logicalCommitted:0});
response=await analyze(userB.token,profileB,"tenant-owner-success");calls=await control("provider-calls",{profileId:profileB,scenario:"tenant-owner-success"});snapshot=await usage(profileB);assert.equal(response.status,200);assert.equal(calls.count,1);assert.equal(committed(snapshot),1);assert.equal(snapshot.technical.length,1);add("tenant-owner-success",{status:200,providerCalls:1,logicalCommitted:1,technicalEvents:1});
const finalState=await control("state");assert.equal(finalState.qaUsers,2);assert.equal(finalState.qaProfiles,2);assert.equal(finalState.profilesWithoutOwner,0);assert.equal(finalState.multipleOwners,0);assert.equal(finalState.ownerMismatch,0);assert.equal(finalState.openPolicies,0);assert.equal(finalState.anonymousPrivilegedTables,0);assertNoSensitive(finalState);
console.log("BRAND_ANALYZE_AUTHENTICATED_RUNTIME: PASS",JSON.stringify({marker,scenarios:evidence.length,evidence,sensitiveFindings:0}));
